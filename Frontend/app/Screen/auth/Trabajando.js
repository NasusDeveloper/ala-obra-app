import React from 'react';
import {Text, View} from 'react-native';

const Trabajando = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text> Página de trabajos en construcción🎉</Text>
    </View>
  );
};

export default Trabajando;