import React from 'react';
import {Text, View} from 'react-native';

const MetodoPago = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text> Página de pagos en construcción🎉</Text>
    </View>
  );
};

export default MetodoPago;